<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gestión de Usuarios</title>
</head>
<body>
  <h1>Gestión de Usuarios</h1>

  <h2>Agregar Usuario</h2>
  <form action="/add/user" method="POST">
    <input type="text" name="nickname" placeholder="Nickname" required>
    <input type="password" name="password" placeholder="Contraseña" required>
    <button type="submit">Agregar Usuario</button>
  </form>

  <h2>Editar Usuario</h2>
  <form action="/edit/user" method="POST">
    <input type="text" name="oldNickname" placeholder="Nickname actual" required>
    <input type="text" name="newNickname" placeholder="Nuevo nickname" required>
    <input type="password" name="newPassword" placeholder="Nueva contraseña" required>
    <button type="submit">Editar Usuario</button>
  </form>

  <h2>Borrar Usuario</h2>
  <form action="/delete/user" method="POST">
    <input type="text" name="nickname" placeholder="Nickname a borrar" required>
    <button type="submit">Borrar Usuario</button>
  </form>

  <h2>Usuarios Registrados</h2>
  <ul>
    <% if (users.length === 0) { %>
      <li>No hay usuarios registrados.</li>
    <% } else { %>
      <% users.forEach(user => { %>
        <li>ID: <%= user.id %> - Nickname: <%= user.name %></li>
      <% }) %>
    <% } %>
  </ul>

  <br>
  <button onclick="window.location.href='/'">Volver a la Página Principal</button>
</body>
</html>
